{
  "installCommand": "npm install",
  "buildCommand": "npm run build -w client",
  "outputDirectory": "client/dist",
  "functions": {
    "api/pet-card.ts": {
      "includeFiles": "api/_lib/fonts/**",
      "maxDuration": 30
    },
    "api/pet.ts": {
      "//": "Sharing warms the card through the CDN first, which on a cold instance is several seconds of wasm start-up — more than the 10s default leaves room for.",
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/card/:user(\\d+).png",
      "destination": "/api/pet-card?u=:user"
    }
  ]
}
